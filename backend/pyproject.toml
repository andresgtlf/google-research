[project]
name = "backend"
version = "0.1.0"
description = "Impact Evidence Research API backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "google-genai>=0.4.0",
    "python-multipart>=0.0.9",
    "markdown>=3.5.0",
]
